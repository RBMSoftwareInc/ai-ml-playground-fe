'use client';

import MainLayout from '../../../components/MainLayout';

export default function ConventionalPage() {
  // MainLayout already provides the full layout structure
  // This page just needs to render MainLayout which handles everything
  return <MainLayout />;
}